kind = "web"
previewPath = "/"
title = "Pathwise CC"
version = "1.0.0"
id = "artifacts/pathwise-cc"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19245

[services.development]
run = "pnpm --filter @workspace/pathwise-cc run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pathwise-cc", "run", "build" ]
publicDir = "artifacts/pathwise-cc/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19245"
BASE_PATH = "/"
